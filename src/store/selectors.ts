import { RootState } from './store';

export const modeofoperation = (state: RootState) => state.mental.modeofoperation;
export const language = (state: RootState) => state.mental.language;
export const numberofdigitsfrom = (state: RootState) => state.mental.numberofdigitsfrom;
export const numberofdigitsto = (state: RootState) => state.mental.numberofdigitsto;
export const numberofrows = (state: RootState) => state.mental.numberofrows;

